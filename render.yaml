services:
  - type: web
    name: bonus-hedge-dashboard
    runtime: node
    rootDir: cloud_web_dashboard
    buildCommand: npm install
    startCommand: node server.js
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
