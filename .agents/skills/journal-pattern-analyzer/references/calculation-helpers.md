# Calculation Helpers

For code-execution environments, reuse the shared repo helper at `skills/_lib/calculations.py`.

Relevant functions for this skill:

- `expectancy`
- `kelly_fraction`

When the journal includes structured results, combine these helpers with simple grouped summaries of win rate, average win, average loss, and loss clustering. Keep any conclusions provisional when the sample is small or inconsistently tagged.
