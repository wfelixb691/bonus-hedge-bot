# Calculation Helpers

For code-execution environments, reuse the shared repo helper at `skills/_lib/calculations.py`.

Relevant functions for this skill:

- `risk_reward_ratio`
- `expectancy`

Use the math only as a cross-check for the proposed structure. Do not let a favorable ratio override weak thesis support, event risk, or unrealistic target logic.
